# My Day 運用構成メモ

## 想定する役割分担

### 開発・配備エージェント（Dev/Ops）

- OSSのFork、改修、テスト、ビルド
- GitHubへの反映とサーバへの配備
- 環境変数・秘密情報の管理
- DB・添付ファイルのバックアップ取得
- 障害時の復旧と更新履歴の管理

### 運営エージェント

APIまたはMCP経由で、運営権限の範囲内で次の作業を支援する。

- 勤務枠の作成・調整
- シフト割り当て案の作成
- 希望・勤務時間・人数不足の確認
- 穴のあるシフトの補充案
- お知らせの作成・配信状況確認
- 操作ログや勤務状況の確認

シフト公開、メンバーの利用停止・削除、大量変更、全員へのお知らせ配信などは、
AIが案を作成した後に人間が確認して確定する運用を基本とする。

## バックアップ方針

- ソースコードと正式な運用ルールはGitHubで管理する。
- DBは定期的にローカルへ取得し、別の外部保存先にも複製する。
- 添付ファイルはDBとは別にR2のバックアップ方針を用意する。
- 操作ログは通常のデータと同じく、エクスポート対象として保管する。
- 復旧できることが重要なので、定期的にバックアップからの復元確認を行う。

運営用の一括取得は `GET /api/v1/groups/:groupId/export` を使用する。
このAPIは読み取り専用で、グループの代表管理者または管理者のAPIキーが必要。
現在はJSON形式で、添付ファイルは本体ではなくメタ情報のみを含む。
勤務記録の開始・終了時刻、承認状態、店舗ネットワーク判定も含む。
通信元IPは環境設定で明示的に有効化した場合だけ保存する。

## 運用知識の保存

運営エージェントが蓄積した店舗ごとのルールは、DBの一時的な会話履歴に埋め込まず、
次のような承認済み文書としてバージョン管理する。

- 店舗基本ルール
- シフト作成ルール
- 担当別の必要人数
- 例外日・繁忙日の扱い
- 過去の判断メモ

AIの作業メモと正式ルールは分離し、正式ルールへの反映は人間が確認してから行う。

## APIとMCPの使い分け

- API：バックアップ、JSON・CSVエクスポート、外部システム連携
- MCP：AIが自然言語で情報を確認し、運営操作を組み立てるための窓口

一括バックアップはAPIを基本とし、MCPは必要な情報の検索・確認や、確認付き操作の補助に使う。
