"use client";

import { useEffect, useState } from "react";
import { getLocalUserId, localApiFetch, setLocalUserId } from "./local-api";

type Group = { id: string; name: string; description: string; ownerEmail: string; membership: { role: string; showInPersonal: boolean }; pendingJoin?: boolean };
type MemberPreference = { minDays: number; maxDays: number; minHours: number; maxHours: number; freeComment?: string | null };
type Availability = { dayOfWeek: number; status: string; startTime: string; endTime: string };
type Member = { userEmail: string; displayName?: string | null; adminNote?: string | null; role: string; showInPersonal: boolean; preference?: MemberPreference | null; availability?: Availability[] };
type GroupDetail = { currentEmail: string; group: Group; membership: { role: string; showInPersonal: boolean }; members: Member[]; requests: Array<{ id: string; userEmail: string; status: string }> };

const weekdayLabels = ["日", "月", "火", "水", "木", "金", "土"];
const preferenceStatusLabels: Record<string, string> = { want: "出勤希望", possible: "可能", off: "休み希望", unavailable: "勤務不可" };
const roleLabels: Record<string, string> = { owner: "代表管理者", editor: "管理者", member: "メンバー" };

function formatAvailability(member: Member) {
  if (!member.availability?.length) return "曜日別の上書きなし（基本設定を適用）";
  return member.availability.map((entry) => {
    const time = entry.startTime && entry.endTime ? `${entry.startTime}〜${entry.endTime}` : "終日";
    return `${weekdayLabels[entry.dayOfWeek]}: ${time} ${preferenceStatusLabels[entry.status] ?? entry.status}`;
  }).join("　／　");
}

export default function GroupsPanel({ onChanged, initialGroupId }: { onChanged: () => void; initialGroupId?: string }) {
  const [groups, setGroups] = useState<Group[]>([]); const [selected, setSelected] = useState<GroupDetail | null>(null); const [name, setName] = useState(""); const [joinId, setJoinId] = useState(""); const [notice, setNotice] = useState<string | null>(null); const [userId, setUserId] = useState(getLocalUserId());
  async function loadGroups() { const response = await localApiFetch("/api/groups"); if (response.ok) setGroups((await response.json() as { groups: Group[] }).groups); }
  useEffect(() => { const timer = window.setTimeout(() => void loadGroups(), 0); return () => window.clearTimeout(timer); }, []); useEffect(() => { const group = groups.find((item) => item.id === initialGroupId); if (group) void openGroup(group); }, [groups, initialGroupId]);
  async function createGroup(event: React.FormEvent) { event.preventDefault(); const response = await localApiFetch("/api/groups", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name }) }); if (!response.ok) return setNotice("グループを作成できませんでした"); setName(""); setNotice("グループを作成しました"); await loadGroups(); onChanged(); }
  async function joinGroup(event: React.FormEvent) { event.preventDefault(); const response = await localApiFetch(`/api/groups/${joinId}/join`, { method: "POST" }); setNotice(response.ok ? "参加申請を送りました" : ((await response.json().catch(() => ({})) as { error?: string }).error ?? "参加申請に失敗しました")); setJoinId(""); await loadGroups(); }
  async function openGroup(group: Group) { const response = await localApiFetch(`/api/groups/${group.id}`); if (response.ok) setSelected(await response.json() as GroupDetail); }
  async function updateMember(body: { userEmail: string; role?: string; adminNote?: string }) { if (!selected) return; const response = await localApiFetch(`/api/groups/${selected.group.id}/members`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); if (!response.ok) setNotice(((await response.json().catch(() => ({})) as { error?: string }).error) ?? "メンバー情報を保存できませんでした"); else { setNotice("保存しました"); await openGroup(selected.group); await loadGroups(); onChanged(); } }
  async function handleRequest(requestId: string, action: "approve" | "reject") { if (!selected) return; await localApiFetch(`/api/groups/${selected.group.id}/requests`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ requestId, action }) }); await openGroup(selected.group); await loadGroups(); }
  async function leaveGroup() { if (!selected || !window.confirm("このグループから退会しますか？ownerの場合は別メンバーへ自動で引き継ぎます。")) return; const response = await localApiFetch(`/api/groups/${selected.group.id}/members`, { method: "DELETE" }); setNotice(response.ok ? "グループから退会しました" : "退会できませんでした"); setSelected(null); await loadGroups(); onChanged(); }
  async function deleteGroup() { if (!selected || selected.membership.role !== "owner" || !window.confirm("グループを削除しますか？予定も削除されます。")) return; const response = await localApiFetch(`/api/groups/${selected.group.id}`, { method: "DELETE" }); setNotice(response.ok ? "グループを削除しました" : "グループを削除できませんでした"); setSelected(null); await loadGroups(); onChanged(); }
  const isAdmin = selected?.membership.role === "owner" || selected?.membership.role === "editor";
  return <section className="groups-card"><div className="groups-head"><div><p className="eyebrow">GROUP CALENDARS</p><h2>グループ予定</h2><p>グループの予定は個人カレンダーにも表示されます。</p></div><span className="group-user">{userId}</span></div>{process.env.NEXT_PUBLIC_LOCAL_MODE === "true" && <div className="local-user-switch"><label>開発ユーザー<input value={userId} onChange={(event) => { setUserId(event.target.value); setLocalUserId(event.target.value); }} onBlur={() => { void loadGroups(); onChanged(); }} /></label><small>認証なしのローカルテストです</small></div>}<div className="group-actions"><form onSubmit={createGroup}><input required value={name} onChange={(event) => setName(event.target.value)} placeholder="新しいグループ名" /><button className="primary-button">グループを作成</button></form><form onSubmit={joinGroup}><input required value={joinId} onChange={(event) => setJoinId(event.target.value)} placeholder="グループIDで参加申請" /><button className="ghost-button">参加申請</button></form></div>{notice && <p className="group-notice" role="status">{notice}</p>}<div className="group-list">{groups.length ? groups.map((group) => <article className="group-item" key={group.id}><div><strong>{group.name}</strong><small>ID: {group.id}</small><span>{roleLabels[group.membership.role] ?? group.membership.role}</span></div>{group.pendingJoin ? <em>承認待ち</em> : <button className="ghost-button" onClick={() => void openGroup(group)}>詳細</button>}</article>) : <p className="group-empty">まだ参加しているグループはありません。</p>}</div>{selected && <div className="group-detail"><div className="modal-head"><div><p className="eyebrow">GROUP</p><h3>{selected.group.name}</h3><small>{selected.group.id}</small></div><button className="close-button" onClick={() => setSelected(null)}>×</button></div><p className="member-admin-help">個人カレンダーにはグループのシフトを表示します。ニックネームは各メンバーの「基本設定」から本人が変更します。</p><h4>メンバー</h4><div className="member-cards">{selected.members.map((member) => <article className="member-card" key={member.userEmail}><div className="member-card-head"><div><strong>{member.displayName?.trim() || member.userEmail.split("@")[0]}</strong><small>{member.userEmail}</small></div><div className="member-card-badges">{selected.membership.role === "owner" && member.userEmail !== selected.group.ownerEmail ? <select className="member-role-select" value={member.role} onChange={(event) => void updateMember({ userEmail: member.userEmail, role: event.target.value })} aria-label={`${member.displayName?.trim() || member.userEmail.split("@")[0]}の権限`}><option value="member">メンバー</option><option value="editor">管理者</option></select> : <span className="member-role-badge">{roleLabels[member.role] ?? member.role}</span>}</div></div><div className="member-preference"><div><b>希望日数</b><span>{member.preference ? `${member.preference.minDays}〜${member.preference.maxDays}日／週` : "未設定"}</span></div><div><b>希望時間</b><span>{member.preference ? `${member.preference.minHours}〜${member.preference.maxHours}時間／週` : "未設定"}</span></div></div><div className="member-availability"><b>曜日別の希望</b><p>{member.preference || member.availability?.length ? formatAvailability(member) : "希望情報は未登録です"}</p></div>{member.preference?.freeComment && <div className="member-free-comment"><b>本人のフリーコメント</b><p>{member.preference.freeComment}</p></div>}{isAdmin && <label className="member-admin-note-field">管理者メモ（本人には表示されません）<textarea defaultValue={member.adminNote ?? ""} placeholder="気を付けること、面談メモなど" rows={2} onBlur={(event) => { const value = event.currentTarget.value.trim(); if (value !== (member.adminNote ?? "")) void updateMember({ userEmail: member.userEmail, adminNote: value }); }} /></label>}</article>)}</div>{!isAdmin && <p className="member-privacy-note">他のメンバーの勤務希望は表示せず、本人の情報だけ確認できます。</p>}{selected.membership.role === "owner" && selected.requests.filter((request) => request.status === "pending").length > 0 && <><h4>参加申請</h4>{selected.requests.filter((request) => request.status === "pending").map((request) => <div className="member-row" key={request.id}><span>{request.userEmail}</span><span><button className="small-action" onClick={() => void handleRequest(request.id, "approve")}>承認</button><button className="small-action danger" onClick={() => void handleRequest(request.id, "reject")}>拒否</button></span></div>)}</>}<div className="group-detail-actions"><button className="ghost-button" onClick={() => void leaveGroup()}>グループから退会</button>{selected.membership.role === "owner" && <button className="small-action danger" onClick={() => void deleteGroup()}>グループを削除</button>}</div></div>}</section>;
}
